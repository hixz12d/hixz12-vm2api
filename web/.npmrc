# Vite 8 / vitest 4 load rolldown native bindings. WSL install on this
# tree only fetched linux-x64, so Windows vitest fell through to a missing
# wasm32-wasi package. Keep both host families in the store.
optional=true
